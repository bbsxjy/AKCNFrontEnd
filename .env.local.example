# .env.local.example - 内网穿透配置示例
#
# 使用说明：
# 1. 复制此文件为 .env.local
# 2. .env.local 会覆盖 .env.development 的配置
# 3. .env.local 不会被git提交（已在.gitignore中）
#
# 使用场景：
# - 当需要通过内网穿透访问时，创建 .env.local 并使用下面的配置
# - 默认情况下使用 .env.development 的本地配置

# 内网穿透后端地址
VITE_API_BASE_URL=http://5184vc587bk4.vicp.fun/api/v1

# 如果后端也通过内网穿透，可以配置WebSocket地址
# VITE_WS_BASE_URL=ws://5184vc587bk4.vicp.fun:3001
